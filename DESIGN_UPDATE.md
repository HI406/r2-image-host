# 界面设计更新说明

## 设计风格

采用**现代极简 + 渐变点缀**的设计语言，参考 Vercel、Linear、Notion 的设计风格。

## 配色方案

### 主色调
- **Background**: `#fef6e4` - 温暖的米黄色背景
- **Headline**: `#001858` - 深蓝色标题
- **Paragraph**: `#172c66` - 蓝色段落文字
- **Button**: `#f582ae` - 粉色主按钮
- **Secondary**: `#8bd3dd` - 浅蓝色辅助色
- **Main**: `#f3d2c1` - 浅粉色主色调

### 渐变应用
- 页面背景：米黄色到浅粉色的对角渐变
- 按钮：粉色系渐变 + 浮动阴影效果
- 进度条：粉色到浅蓝色的线性渐变
- 统计卡片：白色到浅粉色的渐变

## 核心改进

### 1. 上传区域 (Upload Zone)
- 3px 浅蓝色虚线边框，hover 时变为粉色
- 大号上传图标 (📁) 带浮动动画
- 拖拽激活时粉色/蓝色渐变背景，放大 2%
- Ctrl+V 粘贴提示

### 2. 文件卡片 (File Cards)
- 卡片化设计，玻璃态毛玻璃效果
- 🆕 缩略图使用 `aspect-ratio: 1/1` 正方形展示
- 🆕 `object-fit: contain` 完整显示，小图不放大
- hover 时上浮 4px
- 🆕 支持复选框批量选择
- 🆕 显示图片宽高尺寸

### 3. 统计信息卡片 (Stats Cards)
- 大号表情图标 + 彩色徽章
- 数字字体 4xl，加粗显示
- 渐变背景 + hover 上浮 4px

### 4. 按钮系统 (Buttons)
- **btn-primary**: 粉色渐变
- **btn-secondary**: 浅蓝渐变
- **btn-danger**: 红粉渐变
- hover 时上浮 1px，阴影增强

### 5. 文件管理面板
- 🆕 每行显示列数可选（2-6列）
- 🆕 全选/取消全选/反向选择按钮
- 🆕 文件列表倒序排列（最新在前）
- 🆕 文件夹支持复选框批量删除

### 6. WebP 转换开关
- 🆕 图标按钮，紧挨上传预览区关闭按钮
- 🆕 激活时 `#f799c0` 粉色，关闭时 `#808080` 灰色
- 🆕 状态保存在 localStorage

### 7. 上传结果
- 🆕 结果卡片置于 drop-zone 上方，不被遮挡
- 🆕 上传成功自动滚动到结果区域
- 🆕 显示 WebP 转换前后大小对比

### 8. 历史记录表格
- 表头渐变背景
- 行 hover 高亮
- 🆕 时间显示浏览器本地时区

## 视觉反馈

**进度条**：粉色到蓝色渐变，8px 高度
**Toast 消息**：深蓝色渐变背景 + 毛玻璃效果
**导航栏**：玻璃态背景 + active 状态底部渐变下划线

## 微动效
- 卡片 hover 上浮 2-4px + 阴影增强
- 按钮 hover 上浮 1px + 阴影增强
- 上传图标 3 秒循环浮动动画
- 所有交互 0.3s 缓动过渡

## 玻璃态设计 (Glassmorphism)
所有卡片元素采用：
- 半透明白色背景 (rgba(255, 255, 255, 0.8-0.9))
- backdrop-filter: blur(20px) 毛玻璃效果
- 浅色边框 (rgba)
- 阴影层次

## 响应式设计
- 移动端：单列布局，小号间距
- 平板：2-4 列网格
- 桌面：3-5 列网格（可选 2-6 列）
- 所有元素保持一致的圆角和间距

## 技术实现
- **框架**：Tailwind CSS + 自定义 CSS
- **动画**：CSS transitions + keyframes
- **特效**：backdrop-filter, gradients, box-shadow
- **交互**：JavaScript 事件监听
