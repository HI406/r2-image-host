# 上传功能更新说明

## 新增功能

### 1. WebP 格式转换
- 上传预览区右上角图标按钮，点击切换开启/关闭
- 开启后图标变为 `#f799c0` 粉色，关闭时 `#808080` 灰色
- 使用客户端 Canvas API 转换，减小文件大小
- SVG/GIF/WebP 文件自动跳过不转换
- 上传成功后显示转换前后大小对比（如 `102.5KB → 45.2KB (减小 56%)`）
- 状态保存在 localStorage，刷新页面保持

### 2. Ctrl+V 粘贴上传
- 支持从剪贴板直接粘贴图片
- 自动检测剪贴板中的图片数据并生成文件名
- 支持多张图片同时粘贴
- 仅在登录后、上传页面、焦点不在文本框时生效

### 3. 上传前确认机制
选择文件后先预览，确认后才上传：
- 显示待上传文件数量
- 2-4列网格展示正方形缩略图
- 每个卡片显示文件名、大小，可单独删除
- 支持批量取消

### 4. 图片尺寸显示
- 文件卡片显示 `📐 宽×高 px`
- 上传时自动从图片读取宽高并存入 R2 customMetadata
- 修复了 `unknown` 类型：R2 list() API 添加 `include: ['httpMetadata', 'customMetadata']`

## 文件管理功能

### 5. 每行显示列数可选
- 搜索栏右侧 `▦` 按钮，点击弹出下拉菜单
- 可选 2-6 列，选择后立即生效
- 状态保存在 localStorage

### 6. 全选 / 取消全选 / 反向选择
- 搜索栏右侧 `■` 按钮，三态切换：
  - ■ 无选择 → 点击全选
  - □ 全部选中 → 点击取消全选
  - ◩ 部分选中 → 点击反向选择
- 点击后 Toast 提示选中数量
- 复选框勾选状态与 state.selected 实时同步

### 7. 文件列表倒序排列
- 最新上传的图片排在最前面

### 8. 文件夹批量删除
- 每个文件夹卡片左上角有复选框
- 点击复选框不进入文件夹，点击其他区域正常导航
- 状态栏显示选中的文件夹数量

## 上传结果优化

### 9. 上传成功自动滚动
- 上传完成后自动 `scrollIntoView` 到结果区域，方便复制链接

### 10. 结果区域位置调整
- upload-result 卡片置于 drop-zone 上方
- 避免硕大的 drop-zone 遮挡链接复制操作

### 11. 预览图完整显示
- 上传结果预览图使用 `object-fit: contain`，完整显示不裁切

## 上传方式

### 点击选择
点击"选择文件"按钮 → 预览 → 确认上传

### 拖拽上传
拖拽图片到上传区域 → 松开 → 预览 → 确认上传

### 粘贴上传
Ctrl+V 粘贴 → 自动预览 → 确认上传（最快方式）

## 支持的文件类型
JPEG、PNG、GIF、WebP、SVG

## 文件大小限制
单文件 ≤ 50MB
